# Development Dockerfile for Frontend
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Expose Vite dev server port
EXPOSE 3000

# Start development server (command overridden in docker-compose.yml)
CMD ["npm", "run", "dev", "--", "--host"]
